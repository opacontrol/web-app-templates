"use strict"

angular
    /**
     * Definition of the application singleton
     */
    .module("templateApp", [])

    /**
     * Main controller
     */
    .controller("mainCtrl", function($scope, TemplateFactory) {
        
    });
